import Component from "$store/components/newSections/ButtonTab.tsx";
import type { Props } from "$store/components/newSections/ButtonTab.tsx";

export default function island({ ...props }: Props) {
  return <Component props={props} />;
}
